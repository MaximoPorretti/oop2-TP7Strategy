package tpStrategy.external;

public interface DistanciaService {
    /**
     * Calcula la distancia en kilómetros entre un origen y un destino.
     */
    double getDistancia(String origen, String destino);
} 